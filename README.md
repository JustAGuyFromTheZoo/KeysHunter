# 🎯 Keyword Hunter

Мощный инструмент для автоматической генерации низкочастотных SEO-запросов на основе анализа ниши. Система самостоятельно создает релевантные семантические ядра и с помощью Keys.so API собирает длинные хвосты для поведенческого продвижения.

## Возможности

- **Интерактивный режим с выбором региона** — удобный бот с меню для выбора города и настроек
- **Мульти-регион поиск** — одновременная обработка нескольких регионов (Москва + СПб + Новосибирск и т.д.)
- **Offline режим** — генерация семантического ядра без обращения к API
- **Умная генерация семян** — ИИ-алгоритм создает 25-150 поисковых фраз на основе краткого описания ниши
- **Многослойная семантика** — комбинирует транзакционные интенты, локализацию, атрибуты продукта, кейсы использования и сезонность
- **Глубокий парсинг** — расширение через Keys.so API с автоматической фильтрацией по частотности и длине запроса
- **Умная дедупликация** — удаление скрытых дублей (перестановки слов, опечатки, вариации)
- **Гибкая фильтрация** — настраиваемые пороги WSK/WS, минус-слова, защита от adult-контента
- **Rate limiting** — автоматическое соблюдение лимитов API (10 req/10 sec) с повторами при ошибках
- **Экспорт данных** — выгрузка результатов в CSV/JSON с полным набором метрик

## Архитектура пайплайна

```
Описание ниши → Генерация семян → Подсказки API → 
→ Расширение ключей → Фильтрация НЧ → Дедупликация → 
→ Валидация → Экспорт (CSV/JSON/TXT)
```

## Примеры запросов

**Интерактивный режим (самый удобный):**

```bash
python main.py -i
```

Откроется меню с выбором региона, настройками и подтверждением параметров.

**Мульти-регион (поиск по нескольким городам):**

```bash
python main.py --niche "доставка суши" --regions "213,2,56" --max-results 2000
```

**Offline режим (без API, только генерация семян):**

```bash
python main.py --niche "ремонт квартир под ключ" --offline --max-results 500
```

**Цветочный бизнес:**

```bash
python main.py --niche "доставка букетов премиум класса в Москве круглосуточно" --wsk 60 --words 4
```

**Недвижимость:**

```bash
python main.py --niche "новостройки бизнес класса с отделкой от застройщика" --base msk --region 213 --wsk 100
```

**E-commerce:**

```bash
python main.py --niche "детская одежда европейских брендов с доставкой" --minus "бесплатно,опт,китай" --format csv
```

## Структура проекта

```
KeysHunter/
├── main.py              # Точка входа с CLI
├── interactive.py       # Интерактивный режим с меню
├── config.py            # Управление конфигурацией
├── api_client.py        # Клиент Keys.so API
├── rate_limiter.py      # Контроль частоты запросов
├── seed_generator.py    # ИИ-генератор семантических ядер
├── keyword_processor.py # Обработка и фильтрация ключей
├── exporter.py          # Экспорт результатов
├── .env.example         # Шаблон конфигурации
└── requirements.txt     # Зависимости
```

## Технологии

- **Python 3.8+** — основной язык разработки
- **Keys.so REST API** — источник данных о поисковых запросах
- **Rate limiting** — умная очередь запросов с экспоненциальными паузами
- **Многопоточность** — параллельная обработка данных где возможно

## Метрики в выходных данных

| Поле | Описание |
|------|----------|
| `word` | Ключевая фраза |
| `wsk` | Точная частотность |
| `ws` | Базовая частотность |
| `numwords` | Количество слов |
| `isquest` | Является ли вопросом |
| `isgeo` | Является ли топонимом |
| `adscnt` | Количество объявлений в контексте |
| `avbid` | Средняя цена клика |
| `docs` | Количество документов в выдаче |

## Интеллектуальная фильтрация

Система автоматически исключает:
- Информационные запросы без коммерческого интента
- Высокочастотные «головы» без уточнений
- Запросы с нежелательными словами (настраивается)
- Adult и нарушающий контент
- Дубли и перестановки слов

## Стратегия генерации семян

**7 слоев семантики:**

1. **Транзакционный интент** — купить, заказать, цена, стоимость, доставка, акция
2. **Локализация** — город, район, метро, рядом, круглосуточно, с выездом
3. **Атрибуты** — размер, цвет, материал, бренд, модель, комплектация
4. **Кейс использования** — для подарка, для офиса, для свадьбы, срочно
5. **Ограничения** — недорого, рассрочка, гарантия, официально, с документами
6. **Форма запроса** — как выбрать, где купить, сколько стоит, отзывы
7. **Сезонность** — новый год, черная пятница, 8 марта, день рождения

## Обработка ошибок API

- **202** — ожидание готовности отчета с повторными проверками
- **429** — автоматическая пауза согласно заголовку Retry-After
- **401** — уведомление о неверном токене с остановкой
- **404** — пропуск отсутствующих ресурсов с продолжением работы
- **500** — до 3 повторов с экспоненциальным backoff

## Производительность

- До **10 запросов в секунду** с автоматическим throttling
- Обработка **100+ семян** за один запуск
- Получение **1000+ НЧ-ключей** за сессию
- Кеширование результатов на 24 часа

## Лицензия

MIT — используйте в коммерческих и личных проектах

---

Разработано для профессионалов SEO и performance-маркетинга
